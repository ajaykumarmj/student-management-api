spring:
  datasource:
    url: jdbc:h2:file:C:/Users/ajayg/studentdb
    username: student
    password: student
    driver-class-name: org.h2.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop

  h2:
    console:
      enabled: true
      path: /h2-console
